import React from 'react'

import styles from "./Audio.module.scss";
import classNames from "classnames/bind";
const cx = classNames.bind(styles)
function Audio({listAudio}) {

  return (
    <div></div>
  )
}

export default Audio